- name: Install comms packages
  community.general.homebrew:
    name: "{{ comms }}"
  tags:
    - comms

- name: Install comms casks packages
  community.general.homebrew_cask:
    name: "{{ comms_cask }}"
  tags:
    - comms
